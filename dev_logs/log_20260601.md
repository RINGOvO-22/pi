## 2026-06-01

### Daily Summary

设计了 pi 飞书接入的 SDK 方案。该方案使用 TypeScript bridge，在同一个 Node.js 进程里调用飞书官方 Node SDK 和 pi coding agent SDK。飞书侧优先使用适合对话机器人的高层 `Channel` 模块。当前只完成文档设计，没有实现代码。

### 1. Add pi Feishu SDK bridge design

- 在 `study_area/feishu/pi-feishu-integration-plan.md` 中增加 SDK 版目标、模块结构、session 演进和实施顺序。
- 新增 `study_area/feishu/pi-feishu-bridge-sdk/README.md`，作为后续 SDK 原型的独立设计文档。
- 明确第一步只验证 `createAgentSession()`、`prompt()`、事件订阅和 `dispose()`，暂不接飞书。
- 明确飞书长连接事件回调只做校验、去重和入队，避免 pi 长任务阻塞回调并触发超时重推。
- 明确飞书侧优先使用 `createLarkChannel()`，底层 `WSClient` / `EventDispatcher` 只用于排查长连接问题。
- 修正安全边界说明：长连接模式不开放本地监听端口，迁移到 webhook 后再配置受控监听地址和 HTTPS 公网入口。
